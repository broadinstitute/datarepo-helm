# Default values for create-vault-crd-secrets.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
vaultCrd:
  install: false
## if true uncomment below
##vault-crd:
##  vaultCRD:
##    vaultUrl:
##    # Specifies the used authentication method the following values are allowed: token | serviceAccount
##    vaultAuth:
##    # Token with access to the resources that Vault-CRD shares from Vault to Kubernetes. Required if vaultAuth = token
##    vaultToken:
##    # Should it generate rbac resources
##    rbac: true
secrets:
# list of secrets, paths, and types of secrets to be rendered into kube secret
- name: ## "fakeSecret1"
  path: ## "secret/fake/path"
  type: ## "KEYVALUE" https://vault.koudingspawn.de/supported-secret-types
## - name: ## "fakeSecret2"
##  path: ## "secret/fake/path2"
##  type: ## "KEYVALUE" https://vault.koudingspawn.de/supported-secret-types
