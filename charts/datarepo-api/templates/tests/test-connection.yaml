apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datarepo-api.fullname" . }}-test-connection"
  labels:
{{ include "datarepo-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "datarepo-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
