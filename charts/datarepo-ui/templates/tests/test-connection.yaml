apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datarepo-ui.fullname" . }}-test-connection"
  labels:
{{ include "datarepo-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "datarepo-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
