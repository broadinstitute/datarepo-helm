{{- if .Values.ingress.enabled -}}
{{- $serviceName := include "oidc-proxy.fullname" . -}}
{{- $servicePort := .Values.service.port -}}
{{- $paths := .Values.ingress.paths -}}
{{- if .Values.ingress.frontendConfig }}
---
apiVersion: networking.gke.io/v1beta1.FrontendConfig
kind: FrontendConfig
metadata:
  name: {{ .Values.ingress.frontendConfig }}
spec:
  sslPolicy: {{ .Values.ingress.frontendConfig }}
{{- end }}
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
{{- if .Values.ingress.frontendConfig }}
    networking.gke.io/v1beta1.FrontendConfig: {{ .Values.ingress.frontendConfig | quote }}
{{- end }}
    kubernetes.io/ingress.allow-http: {{ .Values.ingress.ingressAllowHttp | quote }}
{{- if .Values.ingress.domainName }}
    networking.gke.io/managed-certificates: {{ .Release.Name }}
{{- end }}
{{- if .Values.ingress.annotations }}
{{ toYaml .Values.ingress.annotations | indent 4 }}
{{- end }}
  name: {{ template "oidc-proxy.fullname" . }}
  labels:
    {{- include "oidc-proxy.labels" . | nindent 4 }}
spec:
  rules:
  {{- if .Values.ingress.hosts }}
  {{- range $host := .Values.ingress.hosts }}
    - host: {{ $host }}
      http:
        paths:
  {{- range $p := $paths }}
          - path: {{ $p }}
            backend:
              serviceName: {{ $serviceName }}
              servicePort: {{ $servicePort }}
  {{- end -}}
  {{- end -}}
  {{- else }}
    - http:
        paths:
  {{- range $p := $paths }}
          - path: {{ $p }}
            backend:
              serviceName: {{ $serviceName }}
              servicePort: {{ $servicePort }}
  {{- end -}}
  {{- end -}}
  {{- if .Values.ingress.tls }}
  tls:
{{- toYaml .Values.ingress.tls | nindent 4 }}
  {{- end -}}
{{- end -}}
